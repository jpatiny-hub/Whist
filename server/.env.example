DATABASE_URL="file:./dev.db"
JWT_SECRET="change-me-to-a-long-random-string"
PORT=4000
# Comma-separated list of origins allowed to call the API (your web app's URL(s)).
CORS_ORIGIN="http://localhost:5173"
